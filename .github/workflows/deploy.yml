name: Deploy to GitHub Pages

on:
  push:
    branches: ["main"]
  schedule:
    # Pick up documentation changes from Prax without requiring changes there.
    - cron: "17 09 * * *"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Setup Hugo
        uses: peaceiris/actions-hugo@v3
        with:
          hugo-version: "0.160.1"
          extended: true

      - name: Build and validate
        env:
          PRAX_DOCS_CACHE: ${{ runner.temp }}/prax-docs
        run: make ci

      - name: Verify Prax deploy artifact
        run: |
          test -f pages-artifact/index.html
          test -f pages-artifact/blog/knowledge-base/prax/index.html
          test -f pages-artifact/blog/references/prax/index.html
          test -f pages-artifact/blog/prax-docs/prax-docs-manifest.json
          test ! -e pages-artifact/Makefile
          test ! -e pages-artifact/blog-source
          test ! -e pages-artifact/scripts
          # GitHub Pages returns 404 for directories that are not world-traversable.
          python3 - <<'PY'
          import stat
          from pathlib import Path

          for relative in (
              "pages-artifact/blog/knowledge-base/prax",
              "pages-artifact/blog/references/prax",
              "pages-artifact/blog/prax-docs",
          ):
              path = Path(relative)
              mode = path.stat().st_mode
              print(f"{relative} mode={oct(mode & 0o777)}")
              if not ((mode & stat.S_IXOTH) and (mode & stat.S_IROTH)):
                  raise SystemExit(
                      f"error: {relative} mode {oct(mode & 0o777)} is not world-traversable"
                  )
          PY

      - name: Setup Pages
        uses: actions/configure-pages@v6

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v5
        with:
          path: 'pages-artifact'
          # v5 excludes dotfiles by default; Pages needs .nojekyll.
          include-hidden-files: true

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
